# Copy this file to secrets.yaml and replace every placeholder locally.
# secrets.yaml is ignored by Git and must never be committed.

wifi_ssid: "replace-with-wifi-ssid"
wifi_password: "replace-with-wifi-password"
pool_temperature_sensor_api_key: "replace-with-base64-api-encryption-key"
pool_temperature_sensor_ota_password: "replace-with-ota-password"
pool_temperature_sensor_fallback_password: "replace-with-fallback-password"
